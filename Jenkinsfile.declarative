pipeline {
  agent slave1
  environment {
    gradleHome = tool 'gradle4'
  }
  stages {
    stage('checkout') {
      steps {
        checkout scm
      }
    }
    stage('build') {
      steps{
        sh "${gradleHome}/bin/gradle build"
      }
    }
 }
